#!/usr/bin/python3

""" a module that has one class """

class MyList(list):
    """ a list class that has one public instance method """

    def print_sorted(self):
        """ sorts our list """
        m = list[0]
        for i in list:
            if i < m:
                m = i
        return list
     
